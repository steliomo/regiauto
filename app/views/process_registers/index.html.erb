<h1>Listing process_registers</h1>

<table>
  <tr>
    <th>Proprietary</th>
    <th>Vehicle</th>
    <th>Register type</th>
    <th>Process status</th>
    <th>Notes</th>
    <th>Documents</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @process_registers.each do |process_register| %>
  <tr>
    <td><%= process_register.proprietary.name %></td>
    <td><%= process_register.vehicle.name %></td>
    <td><%= process_register.register_type %></td>
    <td><%= process_register.process_status %></td>
    <td><%= process_register.notes %></td>
    <td><%= process_register.documents %></td>
    <td><%= link_to 'Show', process_register %></td>
    <td><%= link_to 'Edit', edit_process_register_path(process_register) %></td>
    <td><%= link_to 'Destroy', process_register, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Process register', new_process_register_path %>
